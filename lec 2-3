#include <iostream>
#define N 1000

struct Cat {
    private:
        // Поля (атрибуты)
        std::string m_name;
        int m_age;
        int m_fun;
        int& m_ref;


    public:
        // Default ctor
        Cat() = default;
        // Member initilazer list
        Cat(const std::string& name, int age, int fun, int& ref):
                m_name(name), m_age(age), m_fun(fun), m_ref(ref) {

            std::cout << "ctor" << std::endl;
        }
        ~Cat() {
            std::cout << "dtor" << std::endl;

        }
        // Method
        void Pet() {
            m_fun++;
        }

        void SetName(const std::string& _name) {
            m_name = _name;
        }
    
};

int main() {
    int x = 5;
    // cat - exampler
    // Cat cat;
    int ref = 10;
    Cat cat2("Shlepa", 1, 10, ref);
    // cat.Pet();
    // cat.SetName("Shusha");

    return 0;
}
